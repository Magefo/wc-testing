import { TheadDirective } from './thead.directive';

describe('TheadDirective', () => {
  it('should create an instance', () => {
    const directive = new TheadDirective();
    expect(directive).toBeTruthy();
  });
});
